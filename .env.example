# AI Agent - Environment Variables
# Copiar este archivo a .env y completar los valores

# ========== CRÍTICO - COMPLETAR ANTES DE DOCKER ==========

# Base de datos PostgreSQL (contraseña para PostgreSQL)
POSTGRES_PASSWORD=tu_contraseña_segura_aqui

# URL del Backend (cambiar localhost por IP en producción)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ========== OPCIONALES ==========

# OpenAI API Key (solo si usas features legales)
OPENAI_API_KEY=sk_test_local

# HuggingFace Token (OPCIONAL - solo si usas modelos privados o quieres más descargas)
# Los modelos por defecto (openai/whisper-base, laion/Empathic-Insight-Voice-Small) son PÚBLICOS
# No necesitas token para descargarlos
HF_TOKEN=

# Modelo de embeddings
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ========== AUTOMÁTICOS - NO TOCAR ==========

# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=ai_agents
POSTGRES_USER=postgres

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false

# Model Loading (false = descargar de HuggingFace)
USE_LOCAL_MODELS=false

# Security
SECRET_KEY=tu-clave-secreta-para-tokens
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# Paths (dentro de Docker, no cambiar)
MODELS_PATH=/app/models
AUDIO_UPLOAD_PATH=/app/uploads
TRANSFORMERS_CACHE=/app/.cache/huggingface
CHROMA_DB_PATH=/app/chroma_db_dir

# Database URL (construida automáticamente)
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/ai_agents

# Node environment
NODE_ENV=production

# Telemetry
NEXT_TELEMETRY_DISABLED=1
PYTHONUNBUFFERED=1
